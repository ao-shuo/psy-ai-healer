spring:
  application:
    name: psy-ai-healer

  # MySQL 配置
  datasource:
    url: jdbc:mysql://47.105.85.69:3306/psy_ai_healer?useSSL=false&serverTimezone=UTC&characterEncoding=utf8&allowPublicKeyRetrieval=true
    username: lyl
    password: 130427Lyl
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      max-lifetime: 120000
      keepalive-time: 30000
      connection-timeout: 30000

  # JPA 配置
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  # H2 控制台（如果需要）
  h2:
    console:
      enabled: true
      path: /h2-console

# JWT 配置
app:
  jwt:
    secret: ${APP_JWT_SECRET:auto-generate}
    expiration-seconds: ${APP_JWT_EXPIRATION:86400}

# H2 数据库配置（注释掉的部分）
# h2 配置示例：
# spring:
#   datasource:
#     url: jdbc:h2:mem:psyaihealer;DB_CLOSE_DELAY=-1;MODE=MySQL
#     driverClassName: org.h2.Driver
#     username: lyl
#     password: 130427Lyl